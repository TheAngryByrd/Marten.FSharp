init:
  - git config --global core.autocrlf input
build_script:
  - cmd: set POSTGRES_PASS=Password12!
  - cmd: build.cmd
test: off
version: 0.0.1.{build}
artifacts:
  - path: bin
    name: bin
services:
  - postgresql95
# environment: 
#   POSTGRES_PASS: "Password12!"
